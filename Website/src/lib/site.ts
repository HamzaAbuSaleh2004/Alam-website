// Single source of truth for the site's public origin.
// ⚠️ Replace with the real production domain before launch — this value is used by
// the sitemap, robots.txt and the structured data (JSON-LD) that AI/search engines read.
export const SITE_URL = "https://www.aalamdrugstore.com";
